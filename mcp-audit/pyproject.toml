[build-system]
requires = ["hatchling>=1.18"]
build-backend = "hatchling.build"

[project]
name = "mcpdone-audit"
version = "0.9.0"
description = "Static analyzer for MCP server repos, Python and TypeScript/JavaScript: 10 checks (BadHost / Starlette CVE-2026-48710, FastMCP wrapper-layer asyncio.run bug, loose tool schemas, subprocess command-injection w/ cross-function taint propagation, destructive-filesystem sinks from tool params, keyword-guarded SQL read-only bypass, and a TS engine covering DNS-rebinding-unprotected HTTP transports / CVE-2025-66414, child_process injection, destructive fs sinks, and unconstrained zod tool schemas)."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
keywords = ["mcp", "model-context-protocol", "audit", "security", "static-analysis", "typescript", "javascript"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Topic :: Security",
  "Topic :: Software Development :: Quality Assurance",
]
dependencies = [
  "packaging>=23.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=7.4",
]

[project.scripts]
mcp-audit = "mcp_audit.cli:main"

[project.urls]
Homepage = "https://mcpdone.com"
Repository = "https://github.com/Alienbushman/mcpdone-samples"
Issues = "https://github.com/Alienbushman/mcpdone-samples/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/mcp_audit"]
