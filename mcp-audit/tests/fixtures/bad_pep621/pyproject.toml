[project]
name = "bad-pep621-app"
version = "0.0.1"
requires-python = ">=3.11"
dependencies = [
  "starlette>=0.36.0",
  "fastapi>=0.110.0",
]

[project.optional-dependencies]
http = ["starlette<1.0.0"]
