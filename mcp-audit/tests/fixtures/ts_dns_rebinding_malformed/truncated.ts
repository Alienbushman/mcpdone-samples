import { StreamableHTTPServerTransport } from "@modelcontextprotocol/sdk/server/streamableHttp.js";
const t = new StreamableHTTPServerTransport({ sessionIdGenerator: undefined });
const s = `unterminated ${x
/* never closed
